# Example showing default confirm prompt
- type: password
  prompt: "Create a new password"
  output: "new_password"
  confirm_password: true
  # No confirm_prompt specified - will use default "Confirm your password"
